#include "main.h"

void translationRPN(stackRPN* stack, stackRPN* RPN) {
    if (stack) {
        printf("1translationRPN\n");  // заглушка
    }
    if (RPN) {
        printf("2translationRPN\n");  // заглушка
    }   
}
